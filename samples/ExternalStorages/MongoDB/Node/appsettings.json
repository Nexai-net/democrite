{
  "Democrite": {

    "Endpoint": {
      "AutoGatewayPort": true
    },

    "Cluster": {

      //Specify the extension to use to join the cluster
      "AutoKey": "Mongo",

      // provide the configuration info
      "ConnectionString": "mongodb://127.0.0.1:27017"
    }
  }
}